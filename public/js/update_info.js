export function updateInfo() {
    console.log("Info update script loaded");
    // Future logic to update info page content dynamically
}

document.addEventListener("DOMContentLoaded", () => {
    updateInfo();
});
